import { View, Text } from "react-native";
import React from "react";

const LandCard = ()=>{
    return(
        <View>
            <Text>hai from the LandCard</Text>
        </View>
    )
}
export default React.memo(LandCard);